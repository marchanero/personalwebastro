---
import ThemeToggle from './ThemeToggle.astro';

const navItems = [
    { href: "#inicio", text: "Inicio" },
    { href: "#investigacion", text: "Investigación" },
    { href: "#docencia", text: "Docencia" },
    { href: "#contacto", text: "Contacto" },
    { href: "/publications", text: "Publicaciones" },
    { href: "/projects", text: "Proyectos" },
    { href: "/personal-projects", text: "Proyectos Personales" }
];
---

<header class="fixed top-0 left-0 right-0 z-50 animate-fade-in border-b border-gray-200/10 dark:border-gray-800/50">
    <div class="absolute inset-0 bg-glass-light dark:bg-glass-dark backdrop-blur-md"></div>
    <div class="container max-w-container mx-auto px-4 sm:px-6 relative">
        <nav class="flex flex-col md:flex-row justify-between items-center py-4 md:h-header">
            <a href="/" class="group flex flex-col items-center md:items-start">
                <span class="font-display text-xl font-bold text-gray-900 dark:text-white group-hover:text-accent dark:group-hover:text-accent-light transition-colors duration-300">
                    Roberto Sánchez Reolid
                </span>
                <span class="text-sm text-gray-600 dark:text-gray-400">
                    Profesor e Investigador
                </span>
            </a>
            <div class="flex flex-col md:flex-row items-center gap-6 md:gap-8 mt-4 md:mt-0">
                <ul class="flex flex-wrap justify-center gap-4 sm:gap-6 md:gap-8">
                    {navItems.map(item => (
                        <li>
                            <a 
                                href={item.href}
                                class="relative py-1 text-gray-600 dark:text-gray-300 hover:text-accent dark:hover:text-accent-light font-medium transition-colors duration-300 after:absolute after:left-0 after:bottom-0 after:h-0.5 after:w-0 after:bg-accent dark:after:bg-accent-light after:transition-all after:duration-300 hover:after:w-full"
                            >
                                {item.text}
                            </a>
                        </li>
                    ))}
                </ul>
                <div class="mt-4 md:mt-0">
                    <ThemeToggle />
                </div>
            </div>
        </nav>
    </div>
</header>